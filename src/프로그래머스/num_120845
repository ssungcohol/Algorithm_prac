class Solution {
    public int solution(int[] box, int n) {
        int answer = 1;
        int s = 0;
        
        for (int i = 0; i < box.length; i++) {
            s = box[i]  / n;
            answer *= s;
        }
        
        return answer;
    }
}

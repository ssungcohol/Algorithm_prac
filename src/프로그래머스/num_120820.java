package 프로그래머스;

class num_120820 {
    public int solution(int age) {
        int answer = 0;
        if (age > 0 && age <= 120);
        age = 2022 - age + 1;
        answer = age;  //있어도 되고, 없어도 됌
        return answer;
    }
}